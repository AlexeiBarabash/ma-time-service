apiVersion: v1
kind: Service
metadata:
  name: {{ include "ma-service.fullname" . }}
  labels:
    {{- include "ma-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ma-service.selectorLabels" . | nindent 4 }}
